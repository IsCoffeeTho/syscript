{
  "name": "syscript",
  "version": "0.0.1",
  "module": "src/main.ts",
  "type": "module",
  "private": true,
  "scripts": {
	"start": "bun run src/main.ts",
	"build": "bun build --minify --sourcemap --outfile=syscript --compile src/main.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
